<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is permitted ONLY for private address ranges, so a user can reach a
  model server on their own machine. Everything else — every cloud provider — stays
  HTTPS-only.

  Deliberately NOT android:usesCleartextTraffic="true" on <application>, which would
  allow plaintext to any host on the internet.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <!-- RFC 1918 private ranges, plus loopback and the Android emulator's alias
             for the host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="true">local</domain>
    </domain-config>
</network-security-config>
