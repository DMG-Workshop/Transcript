include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - build/**
    - android/**
    - ios/**
  language:
    strict-casts: true

linter:
  rules:
    - prefer_final_locals
    - unawaited_futures
