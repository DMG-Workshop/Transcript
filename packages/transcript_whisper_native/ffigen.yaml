# Run with `dart run ffigen --config ffigen.yaml`.
name: TranscriptWhisperNativeBindings
description: |
  Bindings for `src/transcript_whisper_native.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/transcript_whisper_native_bindings_generated.dart'
headers:
  entry-points:
    - 'src/transcript_whisper_native.h'
  include-directives:
    - 'src/transcript_whisper_native.h'
comments:
  style: any
  length: full
